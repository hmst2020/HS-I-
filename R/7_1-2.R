######## 一年後 #############
T <-matrix(     # 建構遞移矩陣物件，函式說明請參閱附錄 A
  data=c(0.97,0.06,  # 2*2 矩陣資料(向量物件)
         0.03,0.94),
  nrow=2,       # 指定2列
  byrow = TRUE  # 依列填滿換列
)
X0 <- matrix(   # 建構初始人口分布狀態
  c(0.65,
    0.35),
  nrow =2,
  byrow =TRUE)
X1 <- T %*% X0  # 求第一年後分布狀態
print(X1)        # 印出一年後結果

######## 三年後 #############
X2 <- T %*% X1
X3 <- T %*% X2
X3
